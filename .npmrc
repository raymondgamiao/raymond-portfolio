; @vercel/analytics 2.x declares an optional peer of vue-router ^4, but this
; project is on vue-router 5. The analytics Vue integration works fine with
; v5; this flag stops npm from failing the install over that range mismatch.
legacy-peer-deps=true
